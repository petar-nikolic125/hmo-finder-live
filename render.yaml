services:
  - type: web
    name: hmo-property-search
    env: node
    plan: starter
    buildCommand: npm install && npm run build && mkdir -p server/public && cp -r dist/public/* server/public/
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production